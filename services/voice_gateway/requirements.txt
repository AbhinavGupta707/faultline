# voice_gateway runtime deps (Cloud Run image)
fastapi
uvicorn[standard]
google-genai          # Gemini Live API on Vertex (live mode only; lazy-imported)
websockets
# tests (test_gateway.py, spike.py) additionally need: httpx, jsonschema, pytest
