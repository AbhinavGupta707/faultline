"""Orchestrator (LlmAgent, gemini-3.1-pro) — owns the plan + approval routing.

Phase 0 stub — Session B implements per impl plan §6. Must iterate
agents.depth.DEPTH_AGENTS after Verify (behaviour identical when the list is empty);
see contracts/components.md §3.
"""
