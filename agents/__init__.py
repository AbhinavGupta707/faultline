"""Faultline agent runtime (Session B) — ADK multi-agent system on Gemini.

Package layout per impl plan §2/§6. Mock-first: ELASTIC_MODE=mock runs the full
pipeline against agents/mocks/elastic_fake.py with zero cloud dependencies.
"""
