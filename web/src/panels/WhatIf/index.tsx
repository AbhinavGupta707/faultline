/** What-If console — scenario form + presets, amber SIMULATED treatment.
 *  Session C2 owns this folder. Phase 0 stub renders the mount. */
export default function WhatIf() {
  return (
    <section className="panel" style={{ padding: 12 }}>
      <div className="eyebrow">What-If</div>
      <p className="dim mono" style={{ fontSize: 12 }}>awaiting C2 — scenario console</p>
    </section>
  );
}
