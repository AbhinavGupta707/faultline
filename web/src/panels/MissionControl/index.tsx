/** Mission Control — live plan, Elastic-flagged tool chips, evidence, approval gate.
 *  Session C2 owns this folder. Phase 0 stub renders the mount. */
export default function MissionControl() {
  return (
    <section className="panel" style={{ padding: 12 }}>
      <div className="eyebrow">Mission Control</div>
      <p className="dim mono" style={{ fontSize: 12 }}>awaiting C2 — plan · tool chips · approval gate</p>
    </section>
  );
}
