/** Decision Log — timestamped narrative with evidence chips + report download.
 *  Session C2 owns this folder. Phase 0 stub renders the mount. */
export default function DecisionLog() {
  return (
    <section className="panel" style={{ padding: 12 }}>
      <div className="eyebrow">Decision Log</div>
      <p className="dim mono" style={{ fontSize: 12 }}>awaiting C2 — evidence-linked timeline</p>
    </section>
  );
}
