/** Action Board — ranked exposures, PO card, live call transcript.
 *  Session C2 owns this folder. Phase 0 stub renders the mount. */
export default function ActionBoard() {
  return (
    <section className="panel" style={{ padding: 12 }}>
      <div className="eyebrow">Action Board</div>
      <p className="dim mono" style={{ fontSize: 12 }}>awaiting C2 — exposures · PO · call</p>
    </section>
  );
}
