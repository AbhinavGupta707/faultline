# STATUS — per-branch heartbeat (append one line per milestone; owned per-branch, never merged across)

2026-06-10 · P0 · Phase 0 bootstrap in progress.
2026-06-11 · C1 · Globe fix round 2 (4 items, all committed, VISUALLY verified in Chrome): G1 continent holes — _full3d on the polygon-fill sublayer wasn't enough; the real fix was dropping the ocean sphere radius to 0.985 so large concave land triangles can't sag below it and get depth-occluded (Africa/Brazil now solid). G2 camera — default globe zoom 2.8 (~85% fill), idle rotation spins longitude only (never auto-zooms-out), fly-to holds focusZoom, user zoom latches + persists. G3 ripple labels — 'Vadodara flood · GDACS' pills, rendered as projected DOM (deck TextLayer doesn't render under GlobeView at all — z-fights coplanar land; collision pass misbehaves). G4 live narration line above the legend from plan.active_step + lastTool. KNOWN GAP: deck TextLayer supplier/product labels are invisible on the globe (work on flat) — globe relies on DOM ripple labels + callouts + gold pulse; flat (recording fallback) has full labels. Dev-server reachability note: Chrome (Windows) can only hit the vite server when it's launched via the Preview MCP (persistent); a `wsl … nohup &` server dies with its WSL session. tsc+build green per item.
2026-06-11 · C1 · UI polish sprint (6 items, all committed): (1) killed world duplication (MapView repeat off); (2) globe mode behind ?view=globe default / ?view=flat fallback — _GlobeView + ocean sphere, idle auto-rotate + eased fly-to-incident, reduced-motion safe; (3) stream-driven narration callouts (detected/traced/approval/secured), max 2, projected to the live viewport; (4) label quality — pills + CollisionFilterExtension + leader-lined product labels + hover tooltips + label tiers; (5) layout — voice → map bottom-center, header note no longer truncates; (6) live intelligence — 14 ambient event blips (relevant ignite coral) + scrolling intel ticker (real headlines) + click-to-fly + window 'faultline:focus' listener for C2 evidence chips; replay drives the feed deterministically. tsc+build green throughout. Note: live GPU render still unverifiable in headless preview (hidden tab pauses rAF); logic verified via in-app module eval against fixtures.
2026-06-10 · C1 · Living map ALIVE on replay. tokens+fonts, stream harness (backlog buffer + pause-at-approval choreography, StrictMode-safe), mapModel reducer (all visuals derived from semantic agent.emit/plan/status), deck.gl hero: world GeoJSON basemap (exact palette, no Maps key), teal ArcLayer supply edges + bloom, coral disruption ripples, product ignite→cool-to-mint, gold scan-pulse following agent focus, mono labels, HUD (mode/phase stepper/legend/status/approval gate). Reduced-motion + responsive + keyboard. tsc+build green; reduceMapState/buildLayers verified against ws_replay.jsonl (mid: 2 ripples, 4 hot edges, granola at_risk/sparkling watch; end: granola secured, Jurong recommended). Live GPU render unverifiable in headless preview (hidden tab pauses rAF) — open http://localhost:5173 in a real browser to see it. Next: live WS swap at S2.
